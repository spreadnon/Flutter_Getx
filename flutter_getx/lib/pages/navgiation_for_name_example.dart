import 'package:flutter/material.dart';

class NavgiationForNameExample extends StatelessWidget {
  const NavgiationForNameExample({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
